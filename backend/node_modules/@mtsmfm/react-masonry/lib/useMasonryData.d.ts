export declare const useMasonlyData: ({ ref, minColumnWidth, gap, itemCount, }: {
    ref: React.MutableRefObject<null>;
    minColumnWidth: number;
    gap: number;
    itemCount: number;
}) => {
    setSizeRatio: (index: number, ratio: number) => void;
    itemPositionMap: Map<number, {
        top: number;
        left: number;
        width: number;
    }>;
    maxHeight: number;
};
