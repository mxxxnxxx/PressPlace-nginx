"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useThrottle = void 0;
const react_1 = require("react");
function useThrottle(func, args, ms) {
    const timerRef = react_1.useRef();
    const argsRef = react_1.useRef(args);
    const [currentValue, setCurrentValue] = react_1.useState();
    const [onceCalled, setOnceCalled] = react_1.useState(false);
    react_1.useEffect(() => {
        argsRef.current = args;
        if (!timerRef.current) {
            timerRef.current = setTimeout(() => {
                setCurrentValue(func(...argsRef.current));
                timerRef.current = undefined;
            }, ms);
        }
    }, args);
    react_1.useEffect(() => {
        return () => {
            if (timerRef.current) {
                clearTimeout(timerRef.current);
            }
        };
    }, []);
    if (!onceCalled) {
        setOnceCalled(true);
        const initialValue = func(...args);
        setCurrentValue(initialValue);
        return initialValue;
    }
    return currentValue;
}
exports.useThrottle = useThrottle;
